Run the python file by openinig the command prompt and type
streamlit run app.py
but before this install streamlit in your system by opening the command prompt
pip install streamlit
-------------------------------------------
In this project
1. We wrote our code in jupyter online file.
2. Saved our model as keras_model.h5
3. Then we created a python file app.py and mostly copied the code from jupyter notebook into the visual studio and loaded the Keras model created there and deployed it one the web.